# Trigger.dev Webapp for Dokploy Stack / Swarm (External Databases)
# =================================================================
# Deploy as Dokploy "Stack" type for Docker Swarm with replicas.
# All configurations are self-contained — no external file mounts needed.
#
# Scaling: Each replica runs a single Node.js process. Scale horizontally
# by increasing TRIGGER_REPLICAS — Swarm handles load balancing.
# Replicas spread across all Swarm nodes (manager + workers).
# Adjust per-replica resources (TRIGGER_CPUS, TRIGGER_MEMORY) to fit your nodes.
#
# NFS Requirement: The shared-data volume uses NFS so the bootstrap worker
# token is accessible from replicas on any Swarm node. Set up NFS server on
# the manager node BEFORE deploying. See server-setup-webapp.md § NFS.
#
# Services: trigger (web app), registry
# Databases are EXTERNAL — deploy trigger-dbs-docker-compose.yml FIRST on the DB server.
#
# This compose connects to external DBs via ${DB_HOST}:port (DB server public IP).
# Set DB_HOST in trigger-webapp.env to the DB server's IP address.
#
# Domain Setup (after deploy):
#   1. In Dokploy UI, add domain for trigger service -> port 3000 (HTTPS)
#   2. In Dokploy UI, add domain for registry service -> port 5000 (HTTPS)
#   3. Set TRIGGER_DOMAIN and REGISTRY_DOMAIN env vars to match
#   4. Redeploy
#
# Worker Token (one-time after first deploy):
#   docker service logs <trigger-service> | grep -A15 "Worker Token"
#   Use this token in your worker's TRIGGER_WORKER_TOKEN env var.
#
# IMPORTANT: Deploy with TRIGGER_REPLICAS=1 first, get worker token, then scale up.

services:
  trigger:
    image: "ghcr.io/triggerdotdev/trigger.dev@sha256:b208b821aebeafe1e814618e92009e104bcb66d7b67d9aee580f5b12ce9391ca"
    user: root
    command: |
      sh -c "chown -R node:node /home/node/shared && exec ./scripts/entrypoint.sh"
    networks:
      - default
      - dokploy-network
    deploy:
      replicas: ${TRIGGER_REPLICAS:-3}
      placement:
        preferences:
          - spread: node.id
      restart_policy:
        condition: on-failure
        delay: 10s
        max_attempts: 10
        window: 120s
      update_config:
        parallelism: 1
        delay: 30s
        order: start-first
      resources:
        limits:
          cpus: "${TRIGGER_CPUS:-2}"
          memory: "${TRIGGER_MEMORY:-5G}"
        reservations:
          cpus: "${TRIGGER_CPUS_RESERVED:-1}"
          memory: "${TRIGGER_MEMORY_RESERVED:-2G}"
    environment:
      # Batch Queue - prevents stuck batches from leaked concurrency tokens
      BATCH_QUEUE_SHARD_COUNT: 2
      BATCH_CONCURRENCY_LIMIT_DEFAULT: 10
      BATCH_QUEUE_CONSUMER_COUNT: 5
      BATCH_QUEUE_CONSUMER_INTERVAL_MS: 100
      # Run Engine - Scaled for heavy worker polling
      RUN_ENGINE_WORKER_COUNT: 50
      RUN_ENGINE_TASKS_PER_WORKER: 50
      RUN_ENGINE_WORKER_CONCURRENCY_LIMIT: 50
      RUN_ENGINE_WORKER_POLL_INTERVAL: 500
      RUN_ENGINE_RATE_LIMIT_MAX: 10000
      RUN_ENGINE_RATE_LIMIT_REFILL_RATE: 3000
      API_RATE_LIMIT_MAX: 5000
      API_RATE_LIMIT_REFILL_RATE: 2000
      DATABASE_CONNECTION_LIMIT: 1500
      DEFAULT_ENV_EXECUTION_CONCURRENCY_LIMIT: 1000
      DEFAULT_ORG_EXECUTION_CONCURRENCY_LIMIT: 3000
      WORKER_CONCURRENCY: 50
      MAXIMUM_DEV_QUEUE_SIZE: 30000
      MAXIMUM_DEPLOYED_QUEUE_SIZE: 150000
      # Queue processing optimization (faster polling for heavy worker load)
      MASTER_QUEUE_CONSUMERS_INTERVAL_MS: 250
      MASTER_QUEUE_CONSUMER_DEQUEUE_COUNT: 50
      MARQS_SHARED_WORKER_QUEUE_CONSUMER_INTERVAL_MS: 100
      MARQS_SHARED_WORKER_QUEUE_MAX_MESSAGE_COUNT: 50
      PROCESS_WORKER_QUEUE_DEBOUNCE_MS: 500
      REMIX_APP_PORT: 3000
      # Domain-based URLs (set TRIGGER_DOMAIN in env)
      APP_ORIGIN: "https://${TRIGGER_DOMAIN}"
      LOGIN_ORIGIN: "https://${TRIGGER_DOMAIN}"
      API_ORIGIN: "https://${TRIGGER_DOMAIN}"
      SESSION_SECRET: "${SESSION_SECRET}"
      MAGIC_LINK_SECRET: "${MAGIC_LINK_SECRET}"
      ENCRYPTION_KEY: "${ENCRYPTION_KEY}"
      MANAGED_WORKER_SECRET: "${MANAGED_WORKER_SECRET}"
      # External databases via DB_HOST (DB server public IP)
      DATABASE_URL: "postgresql://postgres:${POSTGRES_PASSWORD}@${DB_HOST}:5432/${POSTGRES_DB:-trigger}?schema=public&sslmode=disable"
      DIRECT_URL: "postgresql://postgres:${POSTGRES_PASSWORD}@${DB_HOST}:5432/${POSTGRES_DB:-trigger}?schema=public&sslmode=disable"
      REDIS_HOST: "${DB_HOST}"
      REDIS_PORT: 6379
      REDIS_PASSWORD: "${REDIS_PASSWORD}"
      REDIS_TLS_DISABLED: "true"
      ELECTRIC_ORIGIN: "http://${DB_HOST}:3000"
      DEV_OTEL_EXPORTER_OTLP_ENDPOINT: "https://${TRIGGER_DOMAIN}/otel"
      DEPLOY_REGISTRY_HOST: "${REGISTRY_DOMAIN}"
      DEPLOY_REGISTRY_NAMESPACE: "${REGISTRY_NAMESPACE:-trigger}"
      DEPLOY_REGISTRY_USERNAME: "${REGISTRY_USERNAME:-trigger}"
      DEPLOY_REGISTRY_PASSWORD: "${REGISTRY_PASSWORD}"
      OBJECT_STORE_BASE_URL: "http://${DB_HOST}:9000"
      OBJECT_STORE_ACCESS_KEY_ID: "${MINIO_ROOT_USER:-admin}"
      OBJECT_STORE_SECRET_ACCESS_KEY: "${MINIO_PASSWORD}"
      GRACEFUL_SHUTDOWN_TIMEOUT: 30000
      NODE_MAX_OLD_SPACE_SIZE: "${NODE_MAX_OLD_SPACE_SIZE:-4096}"
      # Bootstrap is ENABLED - workers will get their token from this webapp
      TRIGGER_BOOTSTRAP_ENABLED: 1
      TRIGGER_BOOTSTRAP_WORKER_GROUP_NAME: bootstrap
      TRIGGER_BOOTSTRAP_WORKER_TOKEN_PATH: /home/node/shared/worker_token
      CLICKHOUSE_URL: "http://${CLICKHOUSE_ADMIN_USER:-default}:${CLICKHOUSE_PASSWORD}@${DB_HOST}:8123?secure=false"
      CLICKHOUSE_LOG_LEVEL: info
      INTERNAL_OTEL_TRACE_LOGGING_ENABLED: 0
      RUN_REPLICATION_ENABLED: 1
      RUN_REPLICATION_CLICKHOUSE_URL: "http://${CLICKHOUSE_ADMIN_USER:-default}:${CLICKHOUSE_PASSWORD}@${DB_HOST}:8123"
      RUN_REPLICATION_LOG_LEVEL: info
      APP_LOG_LEVEL: info
      TRIGGER_TELEMETRY_DISABLED: 0
      # ── Optional features ──
      # To enable email login, add these lines to the environment section:
      #   WHITELISTED_EMAILS: "user@example.com"
      #   EMAIL_TRANSPORT: "resend"        # resend | smtp | aws-ses
      #   FROM_EMAIL: "noreply@yourdomain.com"
      #   RESEND_API_KEY: "re_xxxxxxxxxxxx"
      #   ALERT_EMAIL_TRANSPORT: "resend"
      #   ALERT_FROM_EMAIL: "noreply@yourdomain.com"
      #   ALERT_RESEND_API_KEY: "re_xxxxxxxxxxxx"
      # To enable Slack integration:
      #   ORG_SLACK_INTEGRATION_CLIENT_ID: "your-client-id"
      #   ORG_SLACK_INTEGRATION_CLIENT_SECRET: "your-client-secret"
      # To auto-promote admins:
      #   ADMIN_EMAILS: ".*@yourcompany\\.com"
    volumes:
      - "shared-data:/home/node/shared"
    healthcheck:
      test:
        - CMD
        - node
        - "-e"
        - "require('http').get('http://127.0.0.1:3000/healthcheck',(r)=>process.exit(r.statusCode===200?0:1)).on('error',()=>process.exit(1))"
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 30s
    logging:
      driver: "json-file"
      options:
        max-size: "50m"
        max-file: "5"

  registry:
    image: "registry:2"
    entrypoint: /bin/sh
    command:
      - -c
      - |
        apk add --no-cache apache2-utils
        mkdir -p /auth
        htpasswd -Bbn "$${REGISTRY_USERNAME}" "$${REGISTRY_PASSWORD}" > /auth/htpasswd
        exec /entrypoint.sh /etc/docker/registry/config.yml
    networks:
      - default
      - dokploy-network
    deploy:
      replicas: 1
      placement:
        constraints:
          - node.role == manager
      restart_policy:
        condition: on-failure
        delay: 5s
        max_attempts: 5
      resources:
        limits:
          cpus: "1"
          memory: "1G"
    environment:
      REGISTRY_USERNAME: ${REGISTRY_USERNAME:-trigger}
      REGISTRY_PASSWORD: "${REGISTRY_PASSWORD}"
      REGISTRY_AUTH: htpasswd
      REGISTRY_AUTH_HTPASSWD_REALM: "Registry Realm"
      REGISTRY_AUTH_HTPASSWD_PATH: /auth/htpasswd
      REGISTRY_HTTP_SECRET: "${REGISTRY_PASSWORD}"
    volumes:
      - "registry-data:/var/lib/registry"
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost:5000/"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    logging:
      driver: "json-file"
      options:
        max-size: "50m"
        max-file: "5"

volumes:
  shared-data:
    driver: local
    driver_opts:
      type: nfs
      o: "addr=${NFS_SERVER_IP:-159.69.194.213},rw,nolock,soft,nfsvers=4"
      device: ":/srv/nfs/trigger-shared"
  registry-data:

networks:
  dokploy-network:
    external: true
